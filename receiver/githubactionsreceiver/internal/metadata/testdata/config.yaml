default:
all_set:
  metrics:
    build.info:
      enabled: true
    workflow.jobs.count:
      enabled: true
    workflow.runs.count:
      enabled: true
    ci.github.workflow.run.path:
      enabled: true
    ci.github.workflow.run.event:
      enabled: true
none_set:
  metrics:
    build.info:
      enabled: false
    workflow.jobs.count:
      enabled: false
    workflow.runs.count:
      enabled: false
    ci.github.workflow.run.path:
      enabled: false
    ci.github.workflow.run.event:
      enabled: false
