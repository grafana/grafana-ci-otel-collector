# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for dronereceiver metrics.
    type: object
    properties:
      builds_number:
        description: "BuildsNumberMetricConfig provides config for the builds_number metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      repo_info:
        description: "RepoInfoMetricConfig provides config for the repo_info metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      restarts_total:
        description: "RestartsTotalMetricConfig provides config for the restarts_total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for dronereceiver metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
