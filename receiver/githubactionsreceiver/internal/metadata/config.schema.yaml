# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for githubactions metrics.
    type: object
    properties:
      build.info:
        description: "BuildInfoMetricConfig provides config for the build.info metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      workflow.jobs.count:
        description: "WorkflowJobsCountMetricConfig provides config for the workflow.jobs.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      workflow.runs.count:
        description: "WorkflowRunsCountMetricConfig provides config for the workflow.runs.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for githubactions metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
