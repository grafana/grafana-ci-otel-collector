default:
all_set:
  metrics:
    build.info:
      enabled: true
    renovate.prs.count:
      enabled: true
    workflow.jobs.count:
      enabled: true
    workflow.runs.count:
      enabled: true
none_set:
  metrics:
    build.info:
      enabled: false
    renovate.prs.count:
      enabled: false
    workflow.jobs.count:
      enabled: false
    workflow.runs.count:
      enabled: false
